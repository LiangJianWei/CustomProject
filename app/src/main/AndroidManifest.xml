<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liangjianwei.customproject">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <meta-data
        android:name="UMENG_CHANNEL"
        android:value="${CHANNEL_NAME}" />

    <application
        android:name=".Base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.TestHttpActivity" />
        <activity
            android:name=".Activity.ViewPagerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.SuspensionActivity" />
        <activity android:name=".Activity.EventFristActivity" />
        <activity android:name=".Activity.EventSecondActivity" />
        <activity android:name=".Activity.SwipeRefreshLayoutAct" />
        <activity android:name=".Activity.PullRefreshLayoutAct" />
        <activity android:name=".Activity.RecylerViewAct" />
        <activity android:name=".Activity.CardViewAct"></activity>
    </application>

</manifest>
